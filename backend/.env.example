PORT=4000
# Production binds 0.0.0.0 by default so LAN clients can reach the dashboard.
# Set HOST=127.0.0.1 to restrict to this machine only.
HOST=0.0.0.0
MONGO_URI=mongodb://127.0.0.1:27017
MONGO_DB=wty
